-- local meta = getmetatable(Game.Rule)
-- if meta and meta.__index then
--     for key, value in pairs(meta.__index) do
--         if type(value) == "function" then
--             print(key)
--         elseif type(value) =='table' then
--         end
--     end
-- end

